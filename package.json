{
  "name": "karate-klub-sambon",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p public && cp index.html public/index.html && cp .nojekyll public/.nojekyll"
  },
  "devDependencies": {}
}
